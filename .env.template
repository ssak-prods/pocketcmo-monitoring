# Gemini Key for summarization (Do NOT use primary, use fallback key here to protect limits)
GEMINI_API_KEY_FALLBACK=

# Resend API for email alerts (Limit capped tightly at 10/day in code)
RESEND_API_KEY=
RESEND_SENDER=onboarding@resend.dev
